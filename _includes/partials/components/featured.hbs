{{#if showFeatured}}
  <section class="gh-featured gh-outer">
    <div class="gh-featured-inner gh-inner">
      <h2 class="gh-featured-title">Featured</h2>
      <div class="gh-featured-feed">
        {{#each (slice collections.posts 4 10)}}
          {{> "post-card" this imageSizes="80px"}}
        {{/each}}
      </div>
    </div>
  </section>
{{/if}}
