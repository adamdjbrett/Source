---
permalink: /feed/feed.xml
eleventyExcludeFromCollections: true
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ metadata.title }}</title>
  <subtitle>{{ metadata.description }}</subtitle>
  <link href="{{ metadata.url }}/feed/feed.xml" rel="self"/>
  <link href="{{ metadata.url }}/"/>
  <updated>{% if collections.posts[0] %}{{ collections.posts[0].date.toISOString() }}{% endif %}</updated>
  <id>{{ metadata.url }}/</id>
  {% for post in collections.posts %}
    {% if loop.index <= 20 %}
  <entry>
    <title>{{ post.data.title }}</title>
    <link href="{{ metadata.url }}{{ post.url }}"/>
    <updated>{{ post.date.toISOString() }}</updated>
    <id>{{ metadata.url }}{{ post.url }}</id>
    <summary>{{ post.data.description or '' }}</summary>
  </entry>
    {% endif %}
  {% endfor %}
</feed>
